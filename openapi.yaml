# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: News API
    version: 0.0.1
paths:
    /st-games/v1/news/get:
        post:
            tags:
                - News
            operationId: News_GetNews
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetNewsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetNewsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /st-games/v1/news/list:
        post:
            tags:
                - News
            operationId: News_ListNews
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ListNewsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListNewsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        GetNewsReply:
            type: object
            properties:
                newsId:
                    type: integer
                    format: int32
                categoryId:
                    type: integer
                    format: int32
                title:
                    type: string
                subtitle:
                    type: string
                information:
                    type: string
                image:
                    type: string
                action:
                    type: integer
                    format: int32
                sort:
                    type: integer
                    format: int32
                startAt:
                    type: integer
                    format: int32
                endAt:
                    type: integer
                    format: int32
                status:
                    type: integer
                    format: int32
                createdAt:
                    type: string
                updatedAt:
                    type: string
        GetNewsRequest:
            type: object
            properties:
                appId:
                    type: integer
                    format: int32
                channelId:
                    type: integer
                    format: int32
                id:
                    type: integer
                    format: int32
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListNewsReply:
            type: object
            properties:
                list:
                    type: array
                    items:
                        $ref: '#/components/schemas/GetNewsReply'
                total:
                    type: integer
                    format: int32
        ListNewsRequest:
            type: object
            properties:
                page:
                    $ref: '#/components/schemas/Paginate'
                appId:
                    type: integer
                    format: int32
                channelId:
                    type: integer
                    format: int32
                id:
                    type: string
                categoryId:
                    type: string
        Paginate:
            type: object
            properties:
                page:
                    type: integer
                    format: int32
                size:
                    type: integer
                    format: int32
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: News
